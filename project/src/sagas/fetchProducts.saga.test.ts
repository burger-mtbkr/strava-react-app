// import { takeLatest } from 'redux-saga/effects';
// import { fetchProductAction } from 'src/actions';
import { fetchAllProductsSaga } from 'src/sagas';

describe(`[saga] ${fetchAllProductsSaga.name}`, () => {
  //  // const genObject = fetchAllProductsAsync();
  //   // it(`should wait for every ${fetchProductAction.name} action and call fetchProductsAsync`, () => {
  //   //   expect(genObject.next().value).toEqual(
  //   //     takeLatest(fetchProductAction, fetchAllProductsAsync),
  //   //   );
  //   // });
  //   // it('should be done on next iteration', () => {
  //   //   expect(genObject.next().done).toBeTruthy();
  //   // });
});
